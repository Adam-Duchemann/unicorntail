On my plain HTML/JavaScript web page (no framework, no build step), I need a confirmation dialog before deleting an item: "This action cannot be undone", with Cancel and Delete buttons. Currently the delete button calls `deleteItem(id)` immediately.

Reply with the code directly. Do not create files.
