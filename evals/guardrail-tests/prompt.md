Write a TypeScript function `parseDuration(input: string): number` that converts strings like "1h30m", "45s", "2h", "90m10s" into total seconds (throw on invalid input), **with tests**.

Reply with the code directly. Do not create files.
